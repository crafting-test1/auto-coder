import { Provider } from '../Provider.js';

export class JiraProvider extends Provider {
  constructor() {
    super();
    console.log('JiraProvider initialized');
  }

  // Add specific methods related to Jira provider
}
