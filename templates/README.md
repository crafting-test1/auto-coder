This folder MUST be updated manually by developer.

This folder is reserved for user-defined templates and should not be used by the watcher subsystem.

For watcher command executor templates, see `config/event-prompt.example.hbs`.
